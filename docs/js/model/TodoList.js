export class TodoList {
    constructor(list) {
        this.list = list !== null && list !== void 0 ? list : new Array();
    }
    /** リストを取得する。 */
    get(index) {
    }
    /** リストにデータを追加する。 */
    append(list) {
    }
    /** タイトルで検索する。 */
    searchWithTitle(title) {
    }
}
